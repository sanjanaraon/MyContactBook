/**
 * Created by sanjanar on 21/08/15.
 */
define(['ember'],
    function (Ember) {
       var App=Ember.Application.create({
            LOG_TRANSITIONS:true
       });

        App.Router.map(function(){

        })


        return App;
    }
);
